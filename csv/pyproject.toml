[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "elimity-insights-example-csv-connector"
version = "0.1.0"
description = "Example Elimity Insights custom connector importing from a CSV file"
authors = ["Elimity development team <dev@elimity.com>"]

[tool.poetry.dependencies]
python = "^3.9"
elimity-insights-client = "^9.0.0"
pydantic = "^2.9.2"

[tool.poetry.scripts]
elimity-insights-example-csv-connector = "elimity_insights_example_csv_connector:main"

[tool.poetry.group.dev.dependencies]
black = "^22.10.0"
mypy = "^0.991"
isort = "^5.10.1"

